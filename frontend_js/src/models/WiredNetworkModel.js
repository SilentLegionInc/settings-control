export class WiredNetworkModel {
    constructor(name, id, device, active, autoconnect) {
        this.name = name;
        this.id = id;
        this.device = device;
        this.active = active;
        this.autoconnect = autoconnect;
    }
}
